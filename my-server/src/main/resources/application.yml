server:
  port: 28080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yizhi
    username: 
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml

jwt:
  secret: you_key
  access_token:
    expiration: 3600 # in seconds
  refresh_token:
    expiration: 604800 # in seconds

api:
